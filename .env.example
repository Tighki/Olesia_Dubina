PORT=3000
HOST=0.0.0.0
NODE_ENV=development

SESSION_SECRET=change-me-to-long-random-string
ADMIN_EMAIL=admin@warehouse.local
ADMIN_PASSWORD=Admin123!

# Локально — папка data/. На Render с диском — /var/data (см. render.yaml)
DATABASE_DIR=
DATABASE_PATH=
